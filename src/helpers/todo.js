/** #### Todo
 * - Todo.
 * @param {object} opts Todo
 * @param {string} opts.foo First option
 * @param {number} opts.bar Second option
 * @param {string} [xpx='todo():'] Exception prefix
 */
export const todo = (opts, xpx = 'todo():') => {

    // Dereference the options, to make the code more readable.
    const { foo, bar } = opts;
}
